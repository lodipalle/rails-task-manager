<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "Back to Home", tasks_path %>
<%= link_to "Edit Task", edit_task_path(@task) %>
<%= link_to "Delete task", task_path(@task), method: :delete, data: { confirm: "Are you certain?" } %>